import {
    registerCustomEventListener,
    unregisterCustomEventListener,
    broadcastCustomEvent,
    customEventGlobalNamespace,
} from '../index';

describe('Module: CustomEventHandler', () => {
    describe('broadcastCustomEvent', () => {
        it('should have the customEventGlobalNamespace partially applied')
    })
    describe('registerCustomEventListener', () => {
        it('should expect the eventType and listener to be a string and function respectively');
        it('should add a listener to the listener store');
        it('should return the key to retrieve the listener')
    })
    describe('unregisterCustomEventListener', () => {
        
    })
})